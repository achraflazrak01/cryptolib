# Chosen-Ciphertext (CCA) demos — implemented on Day 4
def run_trial_cca_ctr_fixed_nonce() -> int:
    """Return 1 if attacker wins CCA game against CTR with fixed nonce; else 0."""
    raise NotImplementedError("Week-2 Day 4")

def run_trial_cca_gcm() -> int:
    """Return 1 if attacker wins CCA game against AES-GCM (should be ≈ random); else 0."""
    raise NotImplementedError("Week-2 Day 4")
